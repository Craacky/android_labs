<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardElevation="4dp"
    app:cardCornerRadius="8dp"
    android:layout_margin="8dp"
    android:backgroundTint="#0d1b2aff">

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:spacing="8dp"> <!-- Adding spacing between elements -->
        <!-- TextView to display the ID -->
        <TextView
            android:id="@+id/noteNumber"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ID"
            android:textSize="14sp"
            android:layout_marginEnd="16dp"/>

        <!-- TextView to display the Name -->
        <TextView
            android:id="@+id/noteDescription"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Product Name"
            android:textSize="16sp"
            android:layout_marginStart="16dp" /> <!-- Adding margin between TextViews -->

    </LinearLayout>
</androidx.cardview.widget.CardView>
